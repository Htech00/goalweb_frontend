export function getUserId() {
  let id = localStorage.getItem("goalUserId");
  if (!id) {
    id = "user_" + Math.random().toString(36).substr(2, 9);
    localStorage.setItem("goalUserId", id);
  }
  return id;
}
